/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any specific configurations here if needed, e.g., for image optimization
  // For Gemini image generation, the images are base64 encoded, so no next/image domain config is strictly needed.
};

module.exports = nextConfig;
